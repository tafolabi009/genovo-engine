[package]
name = "genovo-benchmarks"
version.workspace = true
edition.workspace = true

[dev-dependencies]
genovo-core = { workspace = true }
genovo-ecs = { workspace = true }
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "ecs_bench"
harness = false
